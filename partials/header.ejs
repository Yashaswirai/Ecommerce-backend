<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lumina</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-5 py-3">
        <h3 class="m-2 text-3xl font-bold text-solid">Lumina</h3>
        <% var navlinks = typeof navlinks !== "undefined" ? navlinks:true %>
        <% if(navlinks) { %>
            <div class="flex gap-5 text-lg">
            <a href="/shop">Shop</a>
            <a href="/cart">Cart</a>
            <a href="/user/profile">My Account</a>
            <a class="text-red-500" href="/user/logout">Log Out</a>
        </div>
        <% } %>
        
    </nav>